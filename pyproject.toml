[build-system]
requires = ["setuptools>=42", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "sob"
version = "0.1.0"
authors = [
    {name = "Aurélien Cecille"},
]
description = "sob: Self-Supervised Depth Distribution"
readme = "README.md"
